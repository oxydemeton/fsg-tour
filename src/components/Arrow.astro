<svg viewBox="-2 -2 12 12" class="w-8 h-8">
    <line x1="0" y1="8" x2="4" y2="0" class="outliner stroke-[3]" stroke-linecap="round"/>
    <line x1="4" y1="0" x2="8" y2="8" class="outliner" stroke-linecap="round"/>

    <line x1="0" y1="8" x2="4" y2="0" class="stroke-black stroke-2" stroke-linecap="round"/>
    <line x1="4" y1="0" x2="8" y2="8" class="stroke-black stroke-2" stroke-linecap="round"/>
</svg>

<style>
    svg:hover .outliner {
        --stroke-base-color: var(--highlight-color);
    }
    .outliner {
        --stroke-base-color: var(--color-white);
        stroke-width: 3;
        stroke: color-mix(in oklab, var(--stroke-base-color) 80%, transparent);
        @media (prefers-contrast: more) {
            stroke-width: 4;
            stroke: color-mix(in oklab, var(--stroke-base-color) 80%, transparent);
        }
        @media (prefers-contrast: less) {
            stroke: color-mix(in oklab, var(--stroke-base-color) 20%, transparent);
        }
        @media (prefers-reduced-transparency) {
            stroke: var(--stroke-base-color);
        }
    }
    @media (prefers-contrast: less) {
        svg {
            opacity: max(var(--min-transparency), 0.5);
        }
    }
</style>